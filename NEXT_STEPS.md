# 作業状況メモ（2026-05-25更新）

## 残作業: なし（このセッションで完了）

---

## このセッションで完了した作業

### `src/06_predict_from_card.py` — 7箇所のNaN修正

**根本原因**: カード予測（12:00時点、`day.empty=True`フォールバック）では`all_feats_now`にclogit特徴量334列が含まれておらず → parquetから読み込まれない → 18列が100%NaN

#### 修正一覧

| # | 箇所 | 内容 |
|---|------|------|
| 1 | `_need`（parquetパス） | clogit feat_cols + `前走日付` を追加 |
| 2 | `all_feats_now` / `pq_cols`（フォールバック） | clogit feat_cols を追加 |
| 3 | `_mae_base_map`（フォールバック） | `前走日付`,`前走_距離_m`,`前走_surface` を追加 |
| 4 | `_pq` suffix クリーンアップ（フォールバック） | merge後の `_pq` 列でNaN補完→drop |
| 5 | `今回_surface` / `今回_距離_m` 上書き（フォールバック内） | df_latestの前走surface汚染を防ぐため、card列から直接設定 |
| 6 | `今回_surface` / `今回_距離_m` 安全網（共通セクション） | 両パス共通で未設定ならcard列から設定 |
| 7 | レースループ内 前走走破タイム_sec / 今回_surface | 条件を`isna().all()`に修正 + encodingを`{'芝':1.0,'ダ':0.0}`に統一 |

#### SSHセッション（5/24）との競合確認: **なし**
- SSH側の`クラス_rank`フォールバック修正（8スペースインデント、`if day.empty:`ブロック内）
- 今回の修正（フォールバック内・共通セクション）
- インデントレベル・ロジックとも独立、`py_compile`も通過済み

---

## 今後の改善課題（`memory/future_improvements.md`参照）

### 優先度：高
1. **NaN補完にオッズ活用** — NaN率高い馬にcalib代わりに`1/オッズ`を使う
2. **レース内平均imputation** — `_clogit_prepare()`前でNaN列をレース内平均で補完

### 優先度：中
3. **上昇トレンド特徴量強化** — 直近重み付き平均・上昇度・自己ベスト更新フラグ（モデル再学習必要）
4. **集計特徴量拡充** — `近10走_タイム指数平均`、コース別タイム指数平均（モデル再学習必要）

---

## 現在の主要OOS成績（参考）

| 条件 | 件数 | 単勝ROI |
|------|------|---------|
| 全件 | 11,559 | -12.7% |
| EV>0.10 | 2,607 | +2.7% |
| gap>=0.20 + EV>0.10 | 731 | +7.1% |
| gap>=0.20 + オッズ3〜6倍 | 262 | +28.0% |

---

## ファイル構成メモ

- `data/html/newspaper_YYYYMMDD.html` — **手動生成**（出馬表CSV → SSH claude経由）
- `docs/newspaper_YYYYMMDD.html` — **自動生成**（JV-Link APIパイプライン経由）
- `data/raw/cache/YYYYMMDD.cache.pkl` — カード予測キャッシュ
